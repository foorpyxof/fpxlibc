#include <iostream>
#include <stdio.h>
#include <stdint.h>

#define EMPTY_LINE std::cout << std::endl;

#define FPX_EXPECT(v, e) printf(" got:      %s\n(expected: %s)\n", v, e);

#define TRUE 1
#define FALSE 0
